<%- include('../partials/header') %>

<div class="container mt-4">
  <h3>New Expense</h3>

  <form action="/budgets" method="POST" autocomplete="off" class="mt-3">
    <div class="mb-3">
      <label for="budgetName" class="form-label">Expense Name</label>
      <input type="text" name="name" class="form-control" id="budgetName" required>
    </div>
    <div class="mb-3">
      <label for="budgetAmount" class="form-label">Amount</label>
      <div class="input-group">
        <span class="input-group-text">$</span>
        <input type="number" name="amount" class="form-control" id="budgetAmount" required>
      </div>
    </div>
    <div class="mb-3">
      <label for="budgetCategory" class="form-label">Category</label>
      <select name="category" class="form-control" id="budgetCategory" required>
        <option value="housing">Housing</option>
        <option value="groceries">Groceries</option>
        <!-- This is where I can add more categories -->
      </select>
    </div>
    <div class="mb-3">
      <label for="budgetDueDate" class="form-label">Due Date</label>
      <input type="date" name="dueDate" class="form-control" id="budgetDueDate">
    </div>
    <button type="submit" class="btn btn-primary">Save Expense</button>
  </form>
</div>

<%- include('../partials/footer') %>